version: '3.8'

services:
  db:
    image: postgres:15
    container_name: exchange_rate_db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: currency_db
    ports:
      - "5432:5432"